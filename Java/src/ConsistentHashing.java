public class ConsistentHashing {
    /**
     * 1. HashRing
     * 2. ServerMap -> server to virtual node map
     * 3. key distribution map -> keys range to server map
     *
     * methods
     * init
     * add server
     * remove server
     * mapKeys -> maps keys to servers
     * getServer(key)
     */
}
